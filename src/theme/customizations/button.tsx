import type { Theme, Components } from "@mui/material/styles";

/**
 * MUI Button component customizations
 *
 * This file centralizes any Button-level theme overrides (variants, sizes,
 * colors, shapes). Keeping it isolated lets us standardize primary/secondary
 * buttons, icon-only buttons, and spacing consistently across the app.
 *
 * Currently there are no overrides; the export exists so we can add
 * adjustments without touching call sites. Add to `components.MuiButton`
 * in the theme when needed.
 */
/* eslint-disable import/prefer-default-export */
export const buttonCustomizations: Components<Theme> = {};
